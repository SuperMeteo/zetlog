<link rel="stylesheet" href="/style.css">

<div class="dashboard">
  <header class="dash-header">
    <h1>👥 User Management</h1>
    <div class="dash-actions">
      <span class="online">🟢 online</span>
      <a href="/logout" class="logout">Logout</a>
    </div>
  </header>

  <table class="user-table">
    <thead>
      <tr>
        <th>Username</th>
        <th>Email</th>
        <th>Action</th>
      </tr>
    </thead>

    <tbody>
      <% users.forEach(user => { %>
        <tr>
          <td><%= user.username %></td>
          <td><%= user.email %></td>
          <td>
            <a class="edit-btn" href="/users/edit/<%= user._id %>">
              ✏ Edit
            </a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
